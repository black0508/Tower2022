---
description: 只改代码，不直接改 Unity 资产；改代码前先读用户现有改动
alwaysApply: true
---

# Unity 改动范围

## 资产 vs 代码

- **只改动代码**（如 `*.cs`）。
- **可以读取** Unity 资产（`.prefab`、`.unity`、`.asset`、`.meta` 等）以理解配置和引用关系。
- **不要直接修改**任何 Unity 资产，除非用户**明确允许**（例如「帮我改 prefab」「改场景配置」）。

需要资产变更时：在回复里说明要在 Inspector / Unity 编辑器里手动做什么，而不是直接改资产文件。

## 改代码前

- 先读用户当前文件，包括已有注释、TODO、局部改动。
- 做**最小 diff**，不要无必要整文件覆盖。
- **不要删除**用户写的注释；只有逻辑变更导致注释过时时才更新，并简要说明原因。
